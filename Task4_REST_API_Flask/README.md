# Task 4 - REST API using Flask

This project is a simple REST API built with Flask that allows basic CRUD operations for user data using an in-memory data store (dictionary).

---

## 🔧 How to Run

### 1. Install dependencies:
```bash
pip install flask

